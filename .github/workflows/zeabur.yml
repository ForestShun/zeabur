name: zeabur
on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

concurrency:
  group: zeabur
  cancel-in-progress: true 

jobs:
  zeabur:
    runs-on: ubuntu-latest
    steps:
      - name: 恢复缓存 (自动寻找最新版本)
        id: cache-restore
        uses: actions/cache/restore@v4
        with:
          path: z-state.tar.gz
          key: z-id-${{ github.run_id }} 
          restore-keys: |
            z-id-

      - name: 还原与唤醒
        if: steps.cache-restore.outputs.cache-hit == 'true' || steps.cache-restore.outputs.cache-primary-key != steps.cache-restore.outputs.cache-matched-key
        run: |
          echo "解压系统状态包..."
          sudo tar -xzf z-state.tar.gz -C / || true
          
          echo "重载并唤醒 K3s..."
          sudo systemctl daemon-reload
          if [ -f /etc/systemd/system/k3s.service ]; then
              sudo systemctl enable --now k3s || true
              
              # 【新增破冰逻辑】：解决换机器导致的容器假死
              echo "等待 K3s API 准备就绪..."
              for i in {1..30}; do
                if sudo k3s kubectl get node >/dev/null 2>&1; then
                  echo "清理冷冻的僵尸容器，触发原地秒级重建..."
                  # 强制删除所有命名空间下的所有 Pod，K8s 的自愈机制会立刻按新机器的网络状态重建它们
                  sudo k3s kubectl delete pods --all -A
                  break
                fi
                sleep 2
              done
          fi

      - name: 基础安装
        run: |
          curl -fsSL 'https://api.zeabur.com/mesh-server/install.sh?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NzYzMzMwMzUsImlhdCI6MTc3NjMwNDIzNSwiaXNzIjoiemVhYnVyLW1lc2giLCJzZXJ2ZXJfaWQiOiI2OWUwNDA2OWJhZTRhZjU4MjgxNmM4ZmMifQ.MrdUphbWF-kdWhtYOOMpxQ-hQ0jvFDR4LQt3n3vCZw8' | sudo bash
      - name: 维持运行
        run: |
          echo "Mesh IP: $(tailscale ip -4)"
          while true; do
            if ! systemctl is-active --quiet tailscaled; then
                exit 1
            fi
            echo "[$(date -u)] 节点在线 | $(hostname)"
            sleep 3600
          done

      - name: 精准打包
        if: always()
        run: |
          echo "正在打包容器数据及配置..."
          sudo tar --exclude='*.sock' -czf z-state.tar.gz \
            /var/lib/tailscale \
            /usr/local/bin/wonder \
            /etc/ssh/sshd_config.d \
            /usr/local/bin/k3s* \
            /etc/systemd/system/k3s.service \
            /etc/systemd/system/k3s.service.env \
            /etc/rancher/k3s \
            /var/lib/rancher/k3s \
            2>/dev/null || true

      - name: 保存最新缓存
        if: always()
        uses: actions/cache/save@v4
        with:
          path: z-state.tar.gz
          key: z-id-${{ github.run_id }}
